import React from "react";
import { View } from "react-native";
import { Button, Text } from "react-native-paper";

const WorkoutSesion = () => {
	return (
		<View>
			<Text>asa</Text>
			<Button>Add exerises</Button>
		</View>
	);
};

export default WorkoutSesion;
